<Window x:Name="Stamparija" x:Class="Stamparija.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Stamparija"
        mc:Ignorable="d"
        Title="{DynamicResource WindowTitle}" Height="600" Width="1200" FontFamily="Nirmala UI">

    <Window.Background>
        <DynamicResource ResourceKey="WindowBackground" />
    </Window.Background>

    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource Background}"/>
        </Style>
    </Window.Style>

    <Frame Grid.RowSpan="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" x:Name="MainFrame" NavigationUIVisibility="Hidden" />
</Window>
